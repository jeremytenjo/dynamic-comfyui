{
  "name": "body-gen-runpod-template",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@clack/prompts": "0.10.1"
  },
  "scripts": {
    "publish:dockerhub": "node scripts/publish-dockerhub.js --username tenjojeremy",
    "publish:patch": "node scripts/publish-dockerhub.js --username tenjojeremy --bump patch --latest",
    "publish:minor": "node scripts/publish-dockerhub.js --username tenjojeremy --bump minor --latest",
    "publish:major": "node scripts/publish-dockerhub.js --username tenjojeremy --bump major --latest"
  }
}
